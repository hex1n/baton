# Project Profile: {project-name}

> Generated by Planner. Human-owned — edit freely. All roles read this at startup.

## Project Type

{service/API | CLI | library | data pipeline | ML training | embedded | other}

> Influences default batch pattern and verification strategy.

## Build

| Key | Value |
|-----|-------|
| Language | {language and version} |
| Framework | {framework and version, or "none"} |
| Build tool | {tool name and version} |
| Modules | {list or "single module"} |
| Compile / check | `{command}` |
| Test | `{command}` |
| Run | `{command}` |
| Package | `{command}` |

## Test Infrastructure

| Key | Value |
|-----|-------|
| Framework | {test framework} |
| Base class / helpers | {extend or import for new tests, or "none"} |
| Data setup | {fixtures, factories, seed data, test builders, etc.} |
| Mock strategy | {mock library, test doubles, fakes, etc.} |
| Assertion lib | {assertion library or built-in} |
| DB for tests | {in-memory, containers, shared dev, or N/A} |
| Test config | {test profile, env file, or config override} |

## Conventions

### Directory Structure
```
{project root}
  ├── {source dir}/     — {purpose}
  ├── {source dir}/     — {purpose}
  ├── {test dir}/       — {purpose}
  └── {config dir}/     — {purpose}
```

### Response / Output Format
```
{How does the project format API responses, CLI output, or UI state?}
{e.g., wrapper type, envelope pattern, raw JSON, stdout format}
```

### Error Handling
```
Global handler: {file path or pattern}
Error format: {describe structure}
```

### Naming
- Entry points: {naming pattern, e.g., `{Entity}Controller`, `{name}_handler.py`, `{name}.route.ts`}
- Business logic: {naming pattern}
- Data access: {naming pattern}
- Data models: {naming pattern}
- DTOs / schemas: {naming pattern}
- Tests: {naming pattern}

### Transaction / Atomicity
- Default pattern: {how transactions are managed}
- Custom configuration: {if any}

### Logging
- Library: {logging library}
- Query / SQL logging: {enabled? in which profile/env?}
- Pattern: {how loggers are created}

## Framework-Specific Patterns

> Patterns specific to this project's framework that Verifier should check during Tier 2.

| Pattern | How to verify | What to watch for |
|---------|--------------|-------------------|
| {e.g., async operations} | {e.g., check thread pool in logs} | {e.g., running on main thread} |
| {e.g., caching} | {e.g., same request twice, check query count} | {e.g., cache not applied} |
| {e.g., event handling} | {e.g., trigger event, check handler} | {e.g., handler not invoked} |

## Verification Checks

> Tier 2 runtime/behavioral checks that Verifier executes during Mode A verification.
> Adapt to your project type: service → API + DB checks; CLI → execution + output checks; pipeline → run + output validation.

| # | Check | Command / Steps | Expected result | Failure level |
|---|-------|----------------|-----------------|---------------|
| 1 | {e.g., app startup} | `{command}` | {e.g., starts within 30s} | blocker |
| 2 | {e.g., readiness} | `{command or endpoint}` | {e.g., returns healthy} | blocker |
| 3 | {e.g., data access patterns} | {e.g., enable query log, count queries} | {e.g., no N+1} | warning |
| 4 | {e.g., atomicity} | {e.g., trigger error mid-operation, check rollback} | {e.g., partial state rolled back} | warning |
| 5 | {e.g., framework pattern} | {e.g., same request twice, check cache hit} | {e.g., data source hit once} | warning |

## Traps

> Files or patterns that are fragile, surprising, or have bitten people before.

| File / Pattern | Trap | Mitigation |
|---------------|------|------------|
| {file or pattern} | {what's dangerous} | {how to handle safely} |

## Verifier Capability

| Capability | Available | Notes |
|-----------|-----------|-------|
| Compile / check | ✅ / ❌ | |
| Tests | ✅ / ❌ | |
| App startup | ✅ / ❌ | {reason if no} |
| DB / services at runtime | ✅ / ❌ | {details or reason if no} |
| **Verifier Mode** | **A / B / C / C+** | {set after first pre-flight} |

## Builder Delegation

> Optional. Records whether Builder may use internal workers for one batch or fix slice at a time.
> This does not add a public Baton role.

| Key | Value |
|-----|-------|
| Delegation | `{allowed / discouraged / disabled}` |
| Default mode | `{inline / advisory / isolated}` |
| Scratch path | `.context/baton/active/batches/` |
| Helper | `bash v2/tools/builder-worker.sh` |
| Notes | `{batch sizing, isolation preference, or "N/A"}` |

## External Reviewer (Mode C+ only)

> Optional. If configured, Mode C upgrades to C+ — production code review is delegated to
> an external reviewer with different blind spots, elevating evidence from L3 to L2.5.
>
> Keep provider / host details here, not in `v2/protocol.md`. Baton reads this section
> through `bash v2/tools/external-review.sh`.

| Key | Value |
|-----|-------|
| Provider | `{provider name or none}` |
| Adapter | `bash v2/tools/external-review.sh` |
| Availability | ✅ / ❌ |
| Start review command | `{command template or N/A}` |
| Start challenge command | `{command template or N/A}` |
| Status command | `{command template or N/A}` |
| Result command | `{command template or N/A}` |
| Cancel command | `{command template or N/A}` |
| Notes | `{auth, limits, or "wrap complex shell in a local helper script"}` |

**Command placeholders:**
```
{job_id}    → Baton job id created by the adapter
{input}     → plan.md or review.md path passed to the adapter
{base}      → optional git base ref
{focus}     → focus prompt text passed by Verifier
{state_dir} → adapter state directory for this job
{repo_root} → repository root
```

If your provider command needs pipes, shell functions, or more than one step, wrap it in a local helper script and point the command row at that script.

If not configured or unavailable, Verifier stays in Mode C (same-model L3 review).

## External Dependencies

> Services this project talks to that may not be available in dev/test.

| Service | Used by | Test strategy |
|---------|---------|---------------|
| {service name} | {which module/component} | {how it's handled in tests} |

## Notes

{Anything else a new developer (or AI) should know before touching this codebase.}
