# Harness 工程：为什么 2026 年最好的 AI 工程师不再写代码

**作者：Nav Toor（@heynavtoor）**
**原文：**[X/Twitter](https://x.com/heynavtoor/status/2037200578842157462)
**发布时间：**2026年3月27日

---

一位研究者用同一个 AI 模型在同一个编码基准测试上测了两次。第一次得分 42%。第二次得分 78%。同一个模型，同一套测试，所有条件相同。

唯一改变的是 **harness（系统骨架）**。

不是提示词，不是模型，不是温度参数。是 harness——围绕 AI 的那套规则、工具、技能、记忆文件和反馈循环系统，告诉它该如何行为。

这是目前 AI 辅助开发中最重要的发现。而几乎没有人在讨论它。

LangChain 独立验证了同样的结论。他们的编码智能体在 Terminal Bench 2.0 上从 30 名开外跃升至前 5，模型没换，只换了 harness。OpenAI 也证明了这一点。他们的 Codex 团队构建了一个超过 100 万行代码的生产应用，**零行由人类手写**。工程师不写代码，他们设计 harness。

设计这些 harness 的技能现在有了名字：**harness 工程（harness engineering）**。这个词几周前被创造出来，已经在 YouTube 和工程博客上获得关注，但几乎没有主流报道。大多数开发者从未听过这个术语。

这即将改变。而最先学会它的开发者将获得极难复制的职业优势。

这是一份完整指南。什么是 harness 工程，为什么它比你用的模型更重要，今天怎么开始做，以及为什么这个零成本技能会让你不可替代。

## 什么是 harness 工程

让我尽量简单地解释。

当你使用 Claude Code、Cursor 或 Codex 这样的 AI 编码智能体时，AI 模型只是系统的一部分。模型是大脑。但大脑需要指令、需要工具、需要规则、需要记忆、需要在错误上线前拦截它们的反馈循环。

模型周围的所有这些东西？那就是 **harness**。

把它想象成骑马。马很强壮，但没有缰绳、马鞍和嚼子，马想去哪就去哪。Harness 就是把那股力量引导到你需要的方向上的东西。

AI 编码智能体也一样。模型很强大，但没有设计良好的 harness，它就会猜测、偏离、反复犯同样的错误，产出看起来漂亮但在生产环境中崩溃的代码。

**Harness 工程是设计和配置这套系统的实践，让你的 AI 编码智能体永远不会犯同样的错误两次。**

Terraform 创始人 Mitchell Hashimoto——业界最受尊敬的工程师之一——这样定义它：「每当你发现智能体犯了一个错误，你就花时间工程化一个解决方案，让智能体永远不会再犯那个错误。」

一句话概括了整个哲学。**不要祈求更好的模型，修好模型周围的系统。**

## 编码智能体 harness 的五个控制杆

每个编码智能体都有五个可调节的配置点。这些就是你的控制杆。拉对了，同一个模型就能产出质量截然不同的代码。

### 1. 系统提示（CLAUDE.md / AGENTS.md）

这是仓库根目录的一个 markdown 文件，在每次会话开始时注入智能体的上下文。它告诉智能体你的代码库是什么、该遵循什么约定、该避免什么。

大多数人要么完全跳过这个文件，要么让 AI 生成它。两者都是错误。苏黎世联邦理工学院的研究测试了 138 个不同仓库的 agent 文件，发现 AI 生成的文件实际上**降低了性能**，同时还多消耗 20% 的 token。人工编写的有帮助，但前提是简洁且具体。

**规则：控制在 60 行以内。** 只包含适用于每个任务的通用指令。不要列目录结构（智能体可以自己发现）。不要写条件规则（「如果做 X 则 Y」会造成混乱）。只写核心要素：你的技术栈、测试命令、编码约定和硬性规则。

### 2. 技能（知识的渐进式披露）

技能是智能体仅在任务匹配时才加载的指令文件。不是把所有东西塞进系统提示，而是把知识拆分成聚焦的模块，智能体按需发现。

比如你可能有一个数据库迁移技能、一个 API 端点创建技能、一个前端组件模式技能。当智能体遇到迁移任务时，自动加载迁移技能，其余的不进上下文窗口。

这叫做**渐进式披露**。智能体以最小上下文启动，按需拉取更多。这保持上下文窗口干净，防止智能体被无关指令搞混。

### 3. MCP 服务器（工具和能力）

Model Context Protocol 服务器扩展了智能体的能力范围，不只是读文件和跑命令。它们可以把你的智能体连接到 Linear（问题追踪）、Sentry（错误监控）、你的数据库（实时查询）或任何其他外部系统。

但注意：**每个 MCP 工具都会增加智能体的系统提示。** 太多工具会造成 HumanLayer 团队所说的「工具抖动（tool thrash）」——智能体浪费时间决定用哪个工具而不是干活。**从两三个开始，只在遇到真正限制时才加。**

### 4. 子智能体（上下文防火墙）

这是大多数人误解的控制杆。子智能体不是搞什么「前端工程师智能体」和「后端工程师智能体」。那行不通。HumanLayer 团队试过然后放弃了。

子智能体的作用是**上下文防火墙**。当主智能体遇到一个会用中间噪音填满上下文窗口的任务时，把它委托给子智能体。子智能体在自己隔离的上下文中运行，完成工作，只返回结果。中间步骤不会污染父线程。

这就是你让主智能体保持在「聪明区间」的方法。Chroma 的研究表明，AI 模型在更长的上下文长度下**性能可测量地下降**。子智能体让你把大问题拆成小的聚焦会话，让模型保持敏锐。

### 5. 钩子（自动化检查点）

钩子是在智能体工作流特定节点自动运行的脚本。它们给非确定性系统添加了确定性控制。

比如，提交前钩子可以在智能体提交代码前运行 linter。完成前钩子可以强制智能体在宣布任务完成前跑测试。循环检测钩子可以在智能体开始反复做同样的修改时拦住它。

LangChain 构建了一个「PreCompletionChecklistMiddleware」，在智能体完成任何任务前拦截它，强制对照原始需求做一次验证。**那个单一钩子是他们整个 harness 中最大的性能提升之一。**

## 为什么 harness 比模型更重要

这是大多数开发者搞错的地方。他们花几个小时争论 Claude 对 GPT 对 Gemini，追逐每次新模型发布，相信下一个版本会修复一切。

数据说的不是这样。

一位研究者在 2026 年 3 月演示了同一个 AI 模型仅因周围 harness 不同就能从 42% 到 78% 大幅波动。这几乎是双倍的性能。**历史上没有任何模型升级交付过 2 倍改进。但设计良好的 harness 能常规做到。**

LangChain 在行业标准 Terminal Bench 2.0 基准上证明了同样的事。同一模型，不同 harness，从 30 名开外跃升至前 5。

OpenAI 自己的 Codex 团队直接说了：「当智能体遇到困难时，我们把它当作信号。我们识别缺少什么——工具、护栏、文档——然后反馈回仓库。」他们不换模型，**他们修 harness**。

模型是引擎。Harness 是方向盘、刹车和道路。你可以有世界上最强大的引擎，没有方向盘，它就撞墙。

## 今天就开始做 harness 工程（零成本）

你不需要新工具，不需要课程，不需要换 AI 编码智能体。你只需要改变面对失败时的反应方式。

### 第一步：改变你的失败反射

旧反射：智能体犯错，你手动修复然后继续。

新反射：智能体犯错，你问**「我怎么确保它永远不会再犯这个错？」**然后把修复编码进你的 harness。

这就是整个心智转变。每个失败都是 harness 缺少某样东西的信号。找到缺少的，加上它，继续前进。智能体再也不会以那种方式失败。

### 第二步：写一份简洁的 CLAUDE.md / AGENTS.md

在仓库根目录创建一个 markdown 文件。控制在 60 行以内。包含你的技术栈、测试命令、硬性规则（「永远不删迁移文件」「提交前必须跑测试」「使用 TypeScript 严格模式」），其他什么都不要。不要目录映射，不要条件逻辑，不要 AI 生成的内容。

### 第三步：构建你的第一个技能

识别代码库中的重复模式——API 端点创建、数据库迁移、组件脚手架。写一份聚焦的指令文件，解释如何正确做，包括边界情况和常见错误。保存为技能文件。智能体在模式匹配时自动加载。

### 第四步：添加一个钩子

从一个提交前钩子开始，运行你的 linter 和测试套件。如果智能体试图提交未通过检查的代码，钩子在到达仓库之前拦住它。一个钩子，巨大的影响。

### 第五步：对上下文密集任务使用子智能体

当你注意到智能体在长任务上失去连贯性时，把它拆成子任务。把每个子任务委托给子智能体。让子智能体在隔离环境中完成工作，只返回结果。你的主线程保持干净。

### 第六步：每周迭代

每周五，复盘本周的失败。每个失败，给你的 harness 添加一条规则、一个技能或一个钩子。每个失败花五分钟做 harness 工程。随时间推移，你的 harness 积累了修复。你的智能体每周都变得更可靠。不是因为模型进步了，而是因为你的系统进步了。

## 没人看到的职业护城河

这对你的职业为什么重要？

**AI 模型正在商品化。** 每家公司都能接触到同样的前沿模型。Claude、GPT、Gemini——对所有人都可用。模型不再是竞争优势。

但设计良好的 harness 是。它特定于你的代码库，特定于你团队的模式，特定于你领域的边界情况。它不能通过下载模型来复制。它是通过数周数月将真实世界的失败编码进一个从中学习的系统来构建的。

**能设计这些 harness 的开发者是公司无法替代的人。** 不是因为他们写最好的代码，而是因为他们设计了让 AI 写出最好代码的系统。

OpenAI 明确说了：工程师的工作不再是写代码，而是**设计环境、明确意图、构建反馈循环**，让智能体可靠地工作。

这就是 harness 工程。而现在学会它的开发者——当这个术语还很新、当这个学科还在成形的时候——将比所有等待的人领先两年。

**提示工程是 2023 年的技能。上下文工程是 2025 年的技能。Harness 工程是 2026 年的技能。**

它的学习成本为零。不需要新工具。每个能使用编码智能体的开发者都可以开始。

唯一的问题是你今天就开始工程化你的 harness，还是继续祈求下一次模型发布来解决一切。

数据已经回答了这个问题。
