.DS_Store
.idea/
.claude/*
!.claude/settings.json
!.claude/skills/
!.claude/agents/
# claude-mem auto-generated context files (root CLAUDE.md is tracked, unaffected)
**/CLAUDE.md
!CLAUDE.md
*.stackdump
CLAUDE.local.md
autoresearch-results.tsv
autoresearch-quality-results.tsv

# Local cross-task lesson index (Gate 2 D-1, 2026-04-05):
# populated by start-task.sh, consumed by baton-explorer / baton-architect.
# Kept out of the repo to allow capturing company-specific or sensitive
# context. Users are responsible for local backup.
knowledge/

# Baton scratch state (canonical control-plane files stay in .harness/)
.context/baton/active/
