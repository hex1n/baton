{
  "eval_id": 3,
  "eval_name": "hook-dispatch-improvement",
  "configuration": "with_skill",
  "expectations": [
    {"text": "Validates input document claims against actual codebase", "passed": true, "evidence": "Caught stale counts: 'Currently: 10 manifest entries, 10 hook scripts (the input document's count of 8/5 is stale — verified against actual manifest.conf)'. Also caught Issue 2 overstated and Issue 3 misattributed."},
    {"text": "Identifies a unifying root cause beyond the listed symptoms", "passed": true, "evidence": "'The core issue is duplicated context derivation, not the five symptoms listed in the input document.' Unified 8/10 hooks' redundant plan discovery + JSON parsing under one root cause."},
    {"text": "Surfaces hidden assumptions about the current architecture", "passed": true, "evidence": "5-row assumption table: 'Each hook must independently derive context', 'Hooks must run in subshells', 'Every hook needs its own fail-open trap', 'Same-event hooks must be separate', 'manifest needs re-parsing every time'."},
    {"text": "Proposes changes that target root cause, not symptoms", "passed": true, "evidence": "Change 1 (pre-derive context) directly addresses root cause. Change 2 (runner function) addresses boilerplate duplication. Changes 3-4 are tactical optimizations, not symptom patches."},
    {"text": "Includes 'what NOT to change' section with reasoning", "passed": true, "evidence": "6-item 'What NOT to Change' section: manifest.conf, subshell isolation, run-hook.cmd, fail-open principle, test infrastructure, error handling granularity. Each with reasoning."},
    {"text": "Provides concrete code examples for proposed changes", "passed": true, "evidence": "~40 lines of bash code examples for Change 1 (pre-derive), Change 2 (runner function), Change 4 (short-circuit). Including jq/awk fallback patterns."}
  ]
}
