# Iteration 2 Modification

**Target dimension**: Efficiency (baseline: 8, iter-1: 8)

**Change**: In Format guidance section, expanded table guidance to add:
```
A table earns its rows through variance — if a column is uniform or restates
inline citations, a summary sentence is better.
```

**Hypothesis**: Both baseline and iter-1 grading flagged redundant tables (test coverage detail, IDE comparison with uniform columns, source files table that duplicates inline citations). Explicit guidance about table variance should produce tighter, more efficient output.

**Cumulative bytes**: 9417 (baseline) → ~9530 (iter-1) → ~9670 (iter-2)
