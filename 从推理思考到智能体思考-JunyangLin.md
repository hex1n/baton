# 从「推理思考」到「智能体思考」

**作者：Junyang Lin（林俊洋）**｜Qwen 团队
**原文：**[X/Twitter](https://x.com/justinlin610/status/2037116325210829168)
**发布时间：**2026年3月26日

---

过去两年重塑了我们评估模型的方式，也改变了我们对模型的期望。OpenAI 的 o1 表明，「思考」可以成为一种一等能力——你可以专门训练它，并将其暴露给用户。DeepSeek-R1 则证明，推理式后训练可以在原始实验室之外被复现和扩展。OpenAI 将 o1 描述为一个通过强化学习训练「先思考再回答」的模型；DeepSeek 则将 R1 定位为一个与 o1 竞争的开源推理模型。

那个阶段很重要。但 2025 年上半年基本上都在围绕**推理思考（reasoning thinking）**——如何让模型花更多推理时计算资源、如何用更强的奖励信号训练它们、如何暴露或控制额外的推理努力。现在的问题是：下一步是什么？我认为答案是**智能体思考（agentic thinking）**——为了行动而思考，在与环境交互的同时持续根据世界的反馈更新计划。

## 1. o1 和 R1 的崛起真正教会了我们什么

第一波推理模型教会我们：如果我们想在语言模型中扩展强化学习，就需要确定性的、稳定的、可扩展的反馈信号。数学、代码、逻辑和其他可验证领域之所以成为核心，是因为这些场景中的奖励信号远强于通用偏好监督。它们让 RL 可以针对正确性（而非合理性）进行优化。基础设施变得至关重要。

一旦模型被训练为通过更长的轨迹进行推理，RL 就不再是监督微调之上的轻量级附加组件——它变成了一个系统问题。你需要大规模的 rollout、高吞吐量的验证、稳定的策略更新、高效的采样。推理模型的出现既是基础设施的故事，也是建模的故事。OpenAI 将 o1 描述为用 RL 训练的推理模型线，DeepSeek R1 后来进一步证实了这个方向，展示了基于推理的 RL 需要多少专门的算法和基础设施工作。**第一个重大转变：从扩展预训练到扩展用于推理的后训练。**

## 2. 真正的问题从来不只是「合并思考和指令模式」

2025 年初，我们 Qwen 团队中很多人都有一个雄心勃勃的想法。理想的系统应该统一思考模式和指令模式，支持可调节的推理力度——类似于低/中/高推理设置。更好的话，它能自动从提示和上下文中推断出合适的推理量，让模型能够决定何时立即回答、何时多思考一会儿、何时在真正困难的问题上投入更多计算。

概念上这是正确的方向。Qwen3 是最清晰的公开尝试之一。它引入了「混合思考模式」，在同一模型家族中支持思考和非思考行为，强调可控的思考预算，并描述了一个包含「思考模式融合」的四阶段后训练流水线。

但合并远比描述困难。**难点在于数据。** 当人们谈论合并思考和指令模式时，往往首先想到模型侧的兼容性：一个检查点能否支持两种模式？一个 chat 模板能否在它们之间切换？一个服务栈能否暴露正确的开关？更深层的问题是，两种模式的数据分布和行为目标有本质区别。

在尝试平衡模型合并与提高后训练数据质量和多样性的过程中，我们并没有把所有事情做对。修订过程中，我们也密切关注用户实际如何使用思考和指令模式。强指令模型通常被奖励直接性、简洁性、格式合规、低延迟——针对重写、标注、模板化支持、结构化提取和运维问答等重复高频的企业任务。强思考模型则被奖励在困难问题上花更多 token、维持连贯的中间结构、探索替代路径，并保留足够的内部计算以有意义地提升最终正确性。

**这两种行为特征相互拉扯。** 如果合并的数据没有被仔细筛选，结果通常是两个方向都平庸：「思考」行为变得嘈杂、臃肿或缺乏决断力，而「指令」行为变得不够精准、不够可靠，且比商业用户实际需要的更昂贵。

分离在实践中仍具吸引力。2025 年晚些时候，在 Qwen3 最初的混合框架之后，2507 版本发布了独立的 Instruct 和 Thinking 更新，包括独立的 30B 和 235B 变体。在商业部署中，大量客户仍然需要高吞吐、低成本、高度可控的指令行为来处理批量操作。对这些场景来说，合并不是明显的收益。分离产品线让团队可以更专注地解决每种模式各自的数据和训练问题。

其他实验室选择了相反的路线。Anthropic 公开主张集成模型理念：Claude 3.7 Sonnet 作为混合推理模型推出，用户可以选择普通响应或扩展思考，API 用户可以设置思考预算。Anthropic 明确表示，他们认为推理应该是一种集成能力而非独立模型。GLM-4.5 也将自己定位为混合推理模型，统一推理、编码和智能体能力；DeepSeek 后来也朝类似方向发展，V3.1 采用了「Think & Non-Think」混合推理。

**关键问题是合并是否有机。** 如果思考和指令只是共存于一个检查点内，但行为上仍像两个笨拙缝合的人格，产品体验就不自然。真正成功的合并需要平滑的推理力度光谱。模型应该能够表达多个级别的力度，理想情况下是自适应选择。GPT 风格的力度控制指向这个方向：一种关于计算的策略，而非二元开关。

## 3. 为什么 Anthropic 的方向是有益的纠偏

Anthropic 围绕 Claude 3.7 和 Claude 4 的公开表述是克制的。他们强调集成推理、用户控制的思考预算、现实世界任务、编码质量，后来还加入了扩展思考期间使用工具的能力。Claude 3.7 被呈现为可控预算的混合推理模型；Claude 4 则进一步允许推理与工具使用交叉，同时 Anthropic 将编码、长时间运行任务和智能体工作流作为主要目标。

**产生更长的推理痕迹并不自动让模型更智能。** 在很多情况下，过多的可见推理表明分配不当。如果模型试图以同样冗长的方式推理所有事情，它可能是在优先级排序上失败、在压缩上失败，或者在行动上失败。Anthropic 的轨迹暗示了一种更有纪律的观点：思考应该由目标工作负载来塑造。如果目标是编码，思考就应该帮助代码库导航、规划、分解、错误恢复和工具编排。如果目标是智能体工作流，思考就应该提升长时间跨度内的执行质量，而非产生令人印象深刻的中间散文。

这种对目标效用的强调指向更大的趋势：**我们正在从训练模型的时代迈向训练智能体的时代。** 我们在 Qwen3 博客中明确写道「我们正从专注训练模型的时代过渡到以训练智能体为中心的时代」，并将未来的 RL 进展与长期推理的环境反馈联系起来。智能体是一个能够制定计划、决定何时行动、使用工具、感知环境反馈、修订策略并在长时间跨度内持续运作的系统。它由与世界的闭环交互来定义。

## 4. 「智能体思考」真正意味着什么

智能体思考是一个不同的优化目标。推理思考通常以最终答案之前内部推理的质量来衡量：模型能否解决定理、写出证明、产生正确代码或通过基准测试。智能体思考则关乎模型能否在与环境交互时持续取得进展。

核心问题从「模型能不能思考足够长？」转变为「模型能不能以维持有效行动的方式思考？」智能体思考必须处理几件纯推理模型基本可以回避的事情：

- 决定何时停止思考并采取行动
- 选择调用哪个工具以及按什么顺序
- 整合来自环境的嘈杂或部分观测
- 在失败后修订计划
- 在多轮次和多工具调用中保持连贯性

**智能体思考是通过行动进行推理的模型。**

## 5. 为什么智能体 RL 基础设施更难

一旦目标从解决基准问题转向解决交互式任务，RL 技术栈就变了。经典推理 RL 的基础设施不够用。在推理 RL 中，你通常可以将 rollout 视为基本自包含的轨迹，配以相对干净的评估器。在智能体 RL 中，策略被嵌入更大的系统中：工具服务器、浏览器、终端、搜索引擎、模拟器、执行沙箱、API 层、记忆系统和编排框架。环境不再是静态验证器——它是训练系统的一部分。

这产生了新的系统需求：**训练和推理必须更干净地解耦。** 没有这种解耦，rollout 吞吐量就会崩溃。考虑一个需要在实时测试环境中执行生成代码的编码智能体：推理端在等执行反馈时停滞，训练端因缺少完成的轨迹而饥饿，整个流水线的 GPU 利用率远低于经典推理 RL 的预期。工具延迟、部分可观测性和有状态环境会放大这些低效。结果是实验速度在远未达到目标能力水平之前就变慢，变得痛苦。

**环境本身也成为了一等研究对象。** 在 SFT 时代，我们痴迷于数据多样性。在智能体时代，我们应该痴迷于环境质量：稳定性、真实性、覆盖度、难度、状态多样性、反馈丰富度、抗利用性，以及 rollout 生成的可扩展性。环境构建已经开始成为一个真正的创业品类，而非副项目。如果智能体在类生产环境中训练，那么环境就是核心能力栈的一部分。

## 6. 下一个前沿是更可用的思考

我的预期是，智能体思考将成为主导的思考形式。我认为它最终可能取代大部分旧的静态独白式推理思考：过长的、孤立的内部痕迹，试图通过输出越来越多的文本来弥补缺乏交互的不足。即使在非常困难的数学或编码任务上，一个真正先进的系统也应该有权搜索、模拟、执行、检查、验证和修订。目标是稳健且高效地解决问题。

训练这类系统最大的挑战是**奖励黑客（reward hacking）**。一旦模型获得有意义的工具访问，奖励黑客就变得更加危险。一个有搜索能力的模型可能在 RL 过程中学会直接查找答案。编码智能体可能利用仓库中的未来信息、滥用日志或发现使任务失效的捷径。一个存在隐藏泄漏的环境可以让策略看起来超人类，而实际上是在训练它作弊。这是智能体时代比推理时代更加微妙的地方。更好的工具让模型更有用，但也扩大了虚假优化的攻击面。我们应该预期下一波严肃的研究瓶颈来自环境设计、评估器鲁棒性、反作弊协议，以及策略与世界之间更有原则的接口。不过方向是明确的。**工具赋能的思考简单地比孤立思考更有用**，且在提升实际生产力方面有好得多的机会。

智能体思考还意味着**系统编排工程（harness engineering）**。核心智能将越来越多地来自多个智能体的组织方式：一个规划和路由工作的编排者，充当领域专家的专业智能体，以及执行更窄任务的子智能体——同时帮助控制上下文、避免污染，并保持不同推理层级之间的分离。**未来是从训练模型到训练智能体，再从训练智能体到训练系统的转变。**

## 结论

推理浪潮的第一阶段建立了重要的东西：当反馈信号可靠且基础设施能够支撑时，语言模型之上的 RL 可以产生质量上更强的认知。

更深层的转变是从推理思考到智能体思考：从思考更长时间到为行动而思考。训练的核心对象已经改变——它是模型加环境的系统，更具体地说，是智能体及其周围的系统骨架。这改变了哪些研究产物最重要：模型架构和训练数据固然重要，但环境设计、rollout 基础设施、评估器鲁棒性，以及多个智能体协调的接口同样重要。它改变了「好的思考」的定义：在现实世界约束下维持行动的最有用轨迹，而非最长或最显眼的。

它也改变了竞争优势的来源。在推理时代，优势来自更好的 RL 算法、更强的反馈信号和更可扩展的训练流水线。**在智能体时代，优势将来自更好的环境、更紧密的训练-推理集成、更强的系统编排工程，以及在模型的决策与这些决策产生的后果之间闭合循环的能力。**
