{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; BATON_BOOTSTRAP=\"$root/spec/bootstrap\" bash \"$root/spec/bootstrap/hooks/post-artifact\" # baton-validate-artifact"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; BATON_BOOTSTRAP=\"$root/spec/bootstrap\" bash \"$root/spec/bootstrap/hooks/pre-transition\" # baton-validate-transition"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; BATON_BOOTSTRAP=\"$root/spec/bootstrap\" bash \"$root/spec/bootstrap/hooks/stop-check\" # baton-validate-state baton-validate-isolation",
            "statusMessage": "Checking harness state",
            "timeout": 30
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; BATON_BOOTSTRAP=\"$root/spec/bootstrap\" bash \"$root/spec/bootstrap/hooks/session-start\" # baton-harness-context",
            "statusMessage": "Loading harness context"
          }
        ]
      }
    ]
  }
}
