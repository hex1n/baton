# Runtime Signals Directory

Recommended contents:

- `sql-log.txt`
- `performance.json`
- `transaction-test.md`
- `actuator-dump.json`

These files should contain raw or lightly normalized evidence collected by the
runtime evaluator. They are inputs to judgment, not the judgment itself.
