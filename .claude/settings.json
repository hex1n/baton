{
  "env": {
    "ENABLE_CLAUDEAI_MCP_SERVERS": "false",
    "ENABLE_LSP_TOOL": "1",
    "CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "75"
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; BATON_BOOTSTRAP=\"$root/spec/bootstrap\" bash \"$root/spec/bootstrap/hooks/post-artifact.sh\" # baton-validate-artifact"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; BATON_BOOTSTRAP=\"$root/spec/bootstrap\" bash \"$root/spec/bootstrap/hooks/pre-transition.sh\" # baton-validate-transition"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; BATON_BOOTSTRAP=\"$root/spec/bootstrap\" bash \"$root/spec/bootstrap/hooks/stop-check.sh\" # baton-validate-state baton-validate-isolation"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "baton-evaluator|baton-verifier",
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; BATON_BOOTSTRAP=\"$root/spec/bootstrap\" bash \"$root/spec/bootstrap/hooks/subagent-stop.sh\" # baton-subagent-stop"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; BATON_BOOTSTRAP=\"$root/spec/bootstrap\" bash \"$root/spec/bootstrap/hooks/session-start.sh\" # baton-harness-context"
          }
        ]
      }
    ]
  }
}
